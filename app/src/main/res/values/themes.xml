<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KkobakKkobak" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 라이트 모드 색상 정의 -->
        <item name="colorPrimary">@color/toss_blue</item>
        <item name="android:windowBackground">@color/light_gray</item>
        <item name="colorSurfaceContainer">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="scrimBackground">#99FFFFFF</item>
    </style>

    <style name="Theme.KkobakKkobak" parent="Base.Theme.KkobakKkobak" />

    <!-- 토스 스타일 버튼 -->
    <style name="TossLikeButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:background">@drawable/button_background_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- 반투명 테마 설정 -->
    <style name="Theme.AppCompat.Translucent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>

